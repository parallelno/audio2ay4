# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Committed datasets (tracked so the training machine has them): corpus/, samples/, tools/.
# Ignore only transient junk that lands inside them:
corpus/**/*.part
corpus/**/_failures.log
**/_scanprobe.log

# Local caches
/.cache/
.cache/

# Training outputs (regenerable: checkpoints retrain in ~15 min; logs are transient)
/checkpoints/
*.log
